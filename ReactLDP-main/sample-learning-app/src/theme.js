import { createMuiTheme } from "@material-ui/core/styles";
import {green} from "@material-ui/core/colors";

//use material ui colors, etc
const theme = createMuiTheme({
    palette: {
        primary: green,
    },
    status: {
    }
})



export default theme;